<!DOCTYPE html>
<html>
  <head>
    <title>BadPitchGenerator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag asset_path('favicon.png') %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;700&family=Work+Sans&display=swap" rel="stylesheet">
  </head>

  <body>
    <% unless notice.nil? %>
      <p class='notice text-center'><%= notice %></p>
    <% end %>
    <% unless alert.nil? %>
      <p class='alert text-center'><%= alert %></p>
    <% end %>
    <%= render 'partials/header' if current_user %>
    <%= render 'partials/example_sentence' if current_user %>
    <%= yield %>
  </body>
</html>
