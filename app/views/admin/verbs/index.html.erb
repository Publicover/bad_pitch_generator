<div class='container'>
  <div class='row'>
    <div class='col text-center'>
      <h3 class='panel-title'>VERBS</h3>
    </div>
  </div>

  <div class='row'>
    <div class='col text-center'>
      <%= render 'partials/search_form', url: admin_verbs_path %>
    </div>
  </div>
  <%= render 'partials/clear_search', path: admin_verbs_path %>


  <table class='table table-hover'>
    <thead>
      <tr>
        <th scope='col'><%= sort_link 'word' %></th>
      </tr>
    </thead>

    <tbody>
      <% @verbs.each do |verb| %>
        <tr>
          <td scope='row'><%= verb.word %></td>
          <td><%= link_to 'Edit', edit_admin_verb_path(verb) %></td>
          <td><%= link_to 'Delete', admin_verb_path(verb), method: :delete %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= render 'partials/pagy_menu' %>
