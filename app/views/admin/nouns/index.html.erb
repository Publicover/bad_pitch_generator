<div class='container'>
  <div class='row'>
    <div class='col text-center'>
      <h3 class='panel-title'>NOUNS</h3>
    </div>
  </div>

  <div class='row'>
    <div class='col text-center'>
      <%= render 'partials/search_form', url: admin_nouns_path %>
    </div>
  </div>
  <%= render 'partials/clear_search', path: admin_nouns_path %>


  <table class='table table-hover'>
    <thead>
      <tr>
        <th scope='col'><%= sort_link 'word' %></th>
      </tr>
    </thead>

    <tbody>
      <% @nouns.each do |noun| %>
        <tr>
          <td scope='row'><%= noun.word %></td>
          <td><%= link_to 'Edit', edit_admin_noun_path(noun) %></td>
          <td><%= link_to 'Delete', admin_noun_path(noun), method: :delete %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= render 'partials/pagy_menu' %>
