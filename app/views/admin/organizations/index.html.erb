<div class='container'>
  <div class='row'>
    <div class='col text-center'>
      <h3 class='panel-title'>ORGANIZATIONS</h3>
    </div>
  </div>

  <div class='row'>
    <div class='col text-center'>
      <%= render 'partials/search_form', url: admin_organizations_path %>
    </div>
  </div>
  <%= render 'partials/clear_search', path: admin_organizations_path %>


  <table class='table table-hover'>
    <thead>
      <tr>
        <th scope='col'><%= sort_link 'word' %></th>
      </tr>
    </thead>

    <tbody>
      <% @organizations.each do |organization| %>
        <tr>
          <td scope='row'><%= organization.word %></td>
          <td><%= link_to 'Edit', edit_admin_organization_path(organization) %></td>
          <td><%= link_to 'Delete', admin_organization_path(organization), method: :delete %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= render 'partials/pagy_menu' %>
